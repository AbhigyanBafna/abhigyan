---
interface Props {
  tag: string;
  active?: boolean;
}

const { tag, active = false } = Astro.props;
---

<a
  href={`/writing/tag/${tag}`}
  class:list={["tag-pill", { "tag-pill--active": active }]}
>
  {tag}
</a>
