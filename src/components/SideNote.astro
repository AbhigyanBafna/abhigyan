---
// Tufte-style margin note. Renders into the right margin on wide screens;
// collapses to an inline aside on narrow screens. The wrapper is inline +
// zero-width so vertical alignment to the source position is automatic.
---

<span class="sn-wrapper">
  <span class="sn"><slot /></span>
</span>
